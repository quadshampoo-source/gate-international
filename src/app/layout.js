import './globals.css';

export const metadata = {
  title: 'Gate International — Istanbul Properties',
  description:
    'A curated portfolio of Istanbul residences along the Bosphorus and at the heart of the new European side.',
};

export default function RootLayout({ children }) {
  return children;
}
